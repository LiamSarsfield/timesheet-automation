# References

No external references — this is a greenfield project.

The only reference artifact is `template.csv` in the project root, which serves as the canonical layout for generated timesheets.
